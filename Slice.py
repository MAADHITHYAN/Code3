str = "PYTHON"

print("str[1:5] = ", str[1:5])

print("str[:6] = ", str[:6])

#characters starting at index 1 and

# defaults to the start of the stri

print("str[1:] = ", str[1:]) # defaults to the end of the stri

print("str[:] = ", str[:])

# defaults to the entire string

print("str[1:20] = ", str[1:20]) # an index that is too big is trui
